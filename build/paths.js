var appRoot = 'src/';
var outputRoot = 'dist/';

module.exports = {
  html: appRoot + '**/*.html',
  output: outputRoot,
  root: appRoot,
  scss: appRoot + '**/*.scss',
  source: appRoot + '**/*.js'
};